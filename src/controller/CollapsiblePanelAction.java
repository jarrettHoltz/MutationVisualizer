package controller;

public enum CollapsiblePanelAction {
	EXPAND_CODE,		//For expanding the view of the code
	EXPAND_COMPARISON,	//For expanding the view of the comparison to the code (e.g. tests or mutants)
	EXPAND_SUMMARY		//For expanding the summary of the currently-viewed object
}
